# KiCad Plugin Requirements
# Only external pip dependencies needed at runtime

# IPC API connection to KiCad
kicad-python>=0.4.0

# Process scanning to detect the active KiCad project.
# Required on Windows; improves reliability on macOS (handles paths with spaces).
psutil

# Bundled via git submodule - not installed via pip:
# - kiutils       (plugins/kiutils/src/kiutils/)
# - easyeda2kicad (plugins/easyeda2kicad/easyeda2kicad/)

# Provided by KiCad itself - not installed via pip:
# - pcbnew   (SWIG bindings)
# - wxPython (GUI)
